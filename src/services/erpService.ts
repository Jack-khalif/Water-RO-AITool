export async function syncQuotationToERP(quotationId: string): Promise<string> {
  // Stub function - integrate with actual ERP API later
  console.log(`Would sync quotation ${quotationId} to ERP`);
  return `ERP-${Date.now()}`;
}
